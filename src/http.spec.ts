import { describe, test } from "bun:test";

describe('api', () => {
  test.todo('should create a new room when POSTing to /room')
  test.todo('should throw an error if user tries to create multiple rooms')

  test.todo('should allow a user to sit in a room after joining')

  test.todo('should return the current room state when rejoining a room')
})